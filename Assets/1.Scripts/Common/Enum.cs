public enum Currency
{
    Gold,
    Ad,
    InApp,
    Dia
}
public enum StatOpType
{
    Add,       // +=
    Subtract,  // -=
    Multiply,  // *=
    Divide     // /=
}
public enum Grade
{
    Normal,
    Rare,
    Unique,
    Legend,
    Myth
}