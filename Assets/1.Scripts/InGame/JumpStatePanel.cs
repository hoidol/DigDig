using UnityEngine;
using UnityEngine.UI;
public class JumpStatePanel : MonoBehaviour
{
    public Image jumpProgressBar;

    void Update()
    {
        //jumpProgressBar.fillAmount = Player.Instance.jumpTimer / Player.Instance.jumpSpeed;
    }
}
